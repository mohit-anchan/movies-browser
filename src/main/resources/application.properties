## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/movies}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:admin}

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

## This is important
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = validate

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Set cache type to redis if you want to enable redis else NONE
spring.cache.type=redis
spring.redis.host=localhost
spring.redis.port=6379
spring.redis.timeout= 2000

#TODO
# Spring boot  -- done
# DB migration (flyway)  -- done
# Load DB config etc config from Env variable  --done
# Unit tests and Mockito
# Deploy to AWS
# CI / CD
# Load static files from S3
# Connect to a Microservice
# linting
# JWT

#Issues
#On "Run Application" Idea runs a new instance instead of re-running existing
